[global]
pid = ${root}/php-pid
error_log = ${root}/out/php-error.log

[www]
listen = ${root}/php-socket
user = ${user}

pm = dynamic
pm.max_children = 8
pm.start_servers = 1
pm.min_spare_servers = 1
pm.max_spare_servers = 5