[global]
pid = ${root}/out/fpm.pid
error_log = ${root}/out/fpm-error.log

[www]
listen = ${root}/fpm-socket
user = ${user}

pm = dynamic
pm.max_children = 8
pm.start_servers = 1
pm.min_spare_servers = 1
pm.max_spare_servers = 5